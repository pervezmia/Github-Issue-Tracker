
*******____________*******---------********

1.Ans: Var, let and const are non primitive data that is not modified after assignment. Let is reassign but not modified like array new value push or pop. Var is hoisting, const  and let is not hoisting. Var maintains global scope on the other hand, let and const maintain block scope.

2.Ans: Rest operator spreads array or object to get individual elements in new array or object like copy. Changing a new array or object does not affect the old array or object using rest operator.

3.Ans:
Map method used to create an new array or object.
Filter method is used for conditionally. It’s return new array or object. For example , An array with some number if you want to get 18 + numbers with new array then filter method used.
For each loop is used for iterate like for of loop. It is not return  new array. If for each loop is kept by a variable. It’s value is undefined. 

4.Ans: Arrow function is a modern function. Without written return to get result by this function. Arrow function is done one line replacing many lines code.

5. `` this is template literals. It is used with strings and variables together. Multiline code and more readable codes used it.
