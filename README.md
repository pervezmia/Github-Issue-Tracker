
*******____________*******---------********

1. ans: 
Var  let and const are used to declare variables in JavaScript.
Var  has function scope and it is hoisted.
Let and const have block scope.
Lat can reassigned but const can't be reassigned after declaration.
Const is commonly used for values that should not change.


2.ans: The rest operator (...) collects multiple elements into a single array.

3. ans: 
Map() creates a new array by applying a function to each element of an array.
Filter creates a new array based on a condition
For each is used to iterate through an array. But it does not return a new array.
 
4. ans: 
Arrow function are a modern way to write function in Java Script.
Provide shorter syntax compared to regular functions.
If the function has only on expression, the return keyword can be omitted.

5. ans: 
Template literals used Backticks(``);
They allow string interpolation and multiline string.

****************

